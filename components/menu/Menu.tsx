import { Logo } from "../icons/Logo";
import styles from "./styles.module.css";

export const Menu = () => {
  return (
    <div className={styles.menu}>
      <Logo />
      <nav></nav>
    </div>
  );
};
